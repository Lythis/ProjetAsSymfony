{% extends "base.html.twig" %}
{% block body %}
    {{ form_start(sportForm) }}
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-12">
                <div class="mb-2">
                    {{ form_row(sportForm.label) }}
                </div>
                <div class="mb-2">
                    {{ form_row(sportForm.submit) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}